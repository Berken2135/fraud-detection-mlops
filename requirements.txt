# Runtime dependencies for src/, api/, pipelines/, monitoring/ and the Docker image.
# Dev-only tools (tests, lint, notebooks) live in requirements-dev.txt so the
# API image doesn't ship pytest/jupyter.

# Core
pandas>=2.2,<3
numpy>=1.26,<2
scikit-learn>=1.4,<1.6
xgboost>=2.0,<3
imbalanced-learn>=0.12,<0.13
joblib>=1.3,<2
matplotlib>=3.8,<4
seaborn>=0.13,<1

# Experiment tracking / registry
mlflow>=2.14,<3

# API
fastapi>=0.110,<1
uvicorn[standard]>=0.29,<1
pydantic>=2.6,<3
psycopg2-binary>=2.9,<3
SQLAlchemy>=2.0,<3

# Orchestration
prefect>=2.19,<3

# Monitoring / drift
evidently>=0.4.25,<0.5
requests>=2.31,<3
